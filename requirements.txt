# ── Core ────────────────────────────────────────────────────────────────
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0

# ── HTTP / networking ──────────────────────────────────────────────────
requests>=2.31
tenacity>=8.2
# Verify TLS against the OS trust store (handles VPN/proxy/AV root CAs that
# certifi's static bundle doesn't include).
truststore>=0.9

# ── Stream resolution ───────────────────────────────────────────────────
yt-dlp>=2024.4.9

# ── Event-text overlay rendering ────────────────────────────────────────
Pillow>=10.0

# ── YouTube Data API v3 (OAuth) — used ONLY to search for the live stream ─
google-api-python-client>=2.120
google-auth>=2.29
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# NOTE: FFmpeg (with libx264 + aac) must be installed on the host PATH.
#   Windows:  winget install Gyan.FFmpeg     (or  choco install ffmpeg-full)
#   Debian :  sudo apt-get install -y ffmpeg
